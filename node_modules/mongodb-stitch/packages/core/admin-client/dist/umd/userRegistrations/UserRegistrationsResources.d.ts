import { Codec } from "mongodb-stitch-core-sdk";
export interface ConfirmationEmail {
    readonly token: string;
    readonly tokenId: string;
}
export declare class ConfirmationEmailCodec implements Codec<ConfirmationEmail> {
    decode(from: any): ConfirmationEmail;
    encode(from: ConfirmationEmail): object;
}
