import { NamedServiceClientFactory } from "mongodb-stitch-react-native-core";
export interface TwilioServiceClient {
    sendMessage(to: string, from: string, body: string, mediaUrl?: string): Promise<void>;
}
export declare namespace TwilioServiceClient {
    const factory: NamedServiceClientFactory<TwilioServiceClient>;
}
