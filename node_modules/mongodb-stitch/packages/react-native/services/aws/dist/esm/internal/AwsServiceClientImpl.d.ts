import { Decoder } from "mongodb-stitch-core-sdk";
import { AwsRequest, CoreAwsServiceClient } from "mongodb-stitch-core-services-aws";
import { AwsServiceClient } from "../AwsServiceClient";
export default class AwsServiceClientImpl implements AwsServiceClient {
    private readonly proxy;
    constructor(proxy: CoreAwsServiceClient);
    execute<T>(request: AwsRequest, decoder?: Decoder<T>): Promise<T>;
}
