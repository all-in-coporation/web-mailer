import { CoreHttpServiceClient, HttpRequest, HttpResponse } from "mongodb-stitch-core-services-http";
import { HttpServiceClient } from "../HttpServiceClient";
export default class HttpServiceClientImpl implements HttpServiceClient {
    private readonly proxy;
    constructor(proxy: CoreHttpServiceClient);
    execute(request: HttpRequest): Promise<HttpResponse>;
}
